<resources>
    <!-- Window/launch theme only; all real theming is done in Compose (ArcaneTheme). -->
    <style name="Theme.ArcaneMobile" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
