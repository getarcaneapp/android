package app.getarcane.android.ui.theme

import androidx.compose.material3.Typography

// Default Material3 type scale (close enough to iOS dynamic type for parity work).
val ArcaneTypography = Typography()
